# minimum required
CMAKE_MINIMUM_REQUIRED(VERSION 3.5.1)

INCLUDE_DIRECTORIES(../src)
LINK_DIRECTORIES(../build/lib)
AUX_SOURCE_DIRECTORY(. SRC_LIST)
ADD_EXECUTABLE(parserTest ${SRC_LIST})
TARGET_LINK_LIBRARIES(parserTest bmath)
